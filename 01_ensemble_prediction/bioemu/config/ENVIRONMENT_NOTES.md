# BioEmu working environment (frozen via `pip freeze`, per install guide Step 6)
# Captured immediately after the smoke test (chignolin) passed.
# Key pins (see README.md for why these specific versions matter):
#   bioemu==1.3.1
#   jax==0.4.35 / jaxlib==0.4.35
#   tensorflow-cpu==2.18.0   (pinned DOWN from latest — see README)
#   torch==2.12.1
#   python==3.11.15
#
# Full contents: see bioemu_working_env.txt (pip freeze) and
# bioemu_environment.yml (conda env export) in this directory —
# both copied verbatim from ~/projects/bioemu/.
