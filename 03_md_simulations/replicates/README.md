# Replicates

Replicate design, confirmed methodology, and the open verification item
(replicate count per AI-seeded system) are documented in
`../production/README.md` rather than duplicated here, since replicates
are a production-stage detail rather than a separate pipeline stage.

See in particular: "Replicate design — confirmed" and the not-yet-
confirmed item on whether all 13 AI-seeded systems received the same
3-replicate treatment verified for `apo_1M47`.
